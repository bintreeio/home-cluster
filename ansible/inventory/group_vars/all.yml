wildcard_domain: home.bintree.io

# Where docker apps live on the host: /docker/<app> (compose + .env) and
# /docker/appdata/<app>/{data,logs} (bind mounts). deployed via deploy-docker-app playbooks and docker files
docker_root: /docker
docker_app_env: {}