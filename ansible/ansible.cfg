[inventory]
enable_plugins = host_list, script, auto, yaml, ini, community.general.proxmox, community.proxmox.proxmox

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = False